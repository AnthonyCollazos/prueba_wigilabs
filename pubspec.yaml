name: prueba_wigilabs
description: Una prueba para Wigilabs.

publish_to: 'none'

version: 0.2.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^2.0.0
  firebase_core: ^1.10.2
  google_sign_in: ^5.0.4
  flutter_facebook_auth: ^3.4.0
  font_awesome_flutter: ^9.1.0
  shared_preferences: ^2.0.6
  rxdart: ^0.27.0
  provider: ^6.0.1
  http: ^0.13.3

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo_blanco.png
    - assets/images/fondo.jpg